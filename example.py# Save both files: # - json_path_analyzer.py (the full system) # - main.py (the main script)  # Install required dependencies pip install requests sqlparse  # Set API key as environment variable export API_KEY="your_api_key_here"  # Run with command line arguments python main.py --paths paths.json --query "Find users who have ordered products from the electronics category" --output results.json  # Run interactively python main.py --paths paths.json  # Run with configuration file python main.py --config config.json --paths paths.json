# Save both files:
# - json_path_analyzer.py (the full system)
# - main.py (the main script)

# Install required dependencies
pip install requests sqlparse

# Set API key as environment variable
export API_KEY="your_api_key_here"

# Run with command line arguments
python main.py --paths paths.json --query "Find users who have ordered products from the electronics category" --output results.json

# Run interactively
python main.py --paths paths.json

# Run with configuration file
python main.py --config config.json --paths paths.json
